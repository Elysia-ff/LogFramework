namespace LogFramework
{
    // TODO (LogFramework) ::
    // If some logs are linked to the table like 'sample table.csv', 
    // Define here then, Add them to LogContainer.GetTableIdxes.
    public enum TableEnumType
    {
        SampleTable
    }
}
